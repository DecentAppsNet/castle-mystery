import SectionVariable from "./SectionVariable";

/** Maps normalized variable names to authored section variables. */
type SectionVariables = {[variable:string]: SectionVariable};

export default SectionVariables;