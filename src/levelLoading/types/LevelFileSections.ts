import LevelFileSection from "./LevelFileSection";

/** Maps normalized top-level section IDs to their parsed content. */
type LevelFileSections = Record<string, LevelFileSection>;

export default LevelFileSections;